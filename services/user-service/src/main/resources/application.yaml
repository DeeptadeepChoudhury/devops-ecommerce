spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://user-db:5432/users_db
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
